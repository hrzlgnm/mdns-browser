# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@dc057d6ab047f38c862870e90c81a192d24ca92e # v2.11.0
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
