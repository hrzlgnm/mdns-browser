# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@ebc8a1fc2797356209b1d729b20c4ad60b9a3445 # v2.8.4
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
