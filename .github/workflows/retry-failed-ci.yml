# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@ebaa3094cb2cbb2a9ed504a5dbfe22ea8c797dcb # v2.8.3
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
