# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@704f0bd55dc47d19ae784fc728498261923a5977 # v2.7.0
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
