# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT-0

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@4c5cacc3feccdd0365f672c3d1da7a2905810794 # v2.4.0
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
