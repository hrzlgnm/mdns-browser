# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@49f43ae74193a73b2c1e1e066bf8f31a53b96a09 # v2.14.3
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
