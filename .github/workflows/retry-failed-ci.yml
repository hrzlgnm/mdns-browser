# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@f85e921e2b4249d5b5b4eca7845481a382fb7a54 # v2.13.1
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
