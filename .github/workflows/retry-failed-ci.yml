# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@ab90f7412a2c2e04df0a675ec57bc9a68b4ba5bc # v2.12.0
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
