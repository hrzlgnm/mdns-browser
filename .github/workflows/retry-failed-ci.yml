# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@0bb9559803ab259be48398d4137dcec658d45bf9 # v2.14.9
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
