# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@d2c23efb5ed3a9f91dd4b2b898db20aeb5aa788f # v2.9.0
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
