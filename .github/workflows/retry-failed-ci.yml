# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@5a1b0f0b3df2b3a73f472ae833f1fbc78d61b815 # v2.14.5
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
