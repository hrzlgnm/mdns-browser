# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@c4634c8090f9c2941bbad01a692e2dd4fc91416c # v2.14.10
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
