# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@03af2e8afc34f86696de4e40b92dda980359c866 # v2.14.0
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
