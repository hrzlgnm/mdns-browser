# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT-0

name: Retry Failed CI on PRs

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@798dbf3ea5e5da61b778f57e22c3c8a3668f1f68 # v2.3.0
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
