# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Retry Failed CI

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  retry-failed:
    uses: hrzlgnm/actions/.github/workflows/retry-failed-ci-reusable.yml@30ff1b2685b9a56d7693655cf248f079be398dac # v2.14.2
    permissions:
      actions: write
      contents: read
    with:
      workflow: ci.yml
      hours: 24
