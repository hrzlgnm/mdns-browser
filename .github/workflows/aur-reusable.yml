# Copyright 2025-2026 hrzlgnm
# SPDX-License-Identifier: MIT

name: Arch linux AUR

on:
  workflow_call:
  workflow_dispatch:
  release:
    types: [released]

jobs:
  release-info:
    permissions:
      packages: read # needed to read release info from GitHub Packages
    runs-on: ubuntu-slim
    outputs:
      version: ${{ steps.version.outputs.version }}
      tagName: ${{ steps.version.outputs.tagName }}
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: 🏷️ Get version info
        id: version
        uses: ./.github/actions/latest-release-info

  lint-scripts:
    name: "🧹 Lint shell scripts"
    uses: hrzlgnm/actions/.github/workflows/shell-lint-reusable.yml@9a8b61da03cbf93775f3fb47f6b393d06d8c62ed # v2.10.0
    with:
      runs-on: ubuntu-latest
      scripts-dir: packaging/aur

  update-aur:
    needs:
      - release-info
      - lint-scripts
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/hrzlgnm/mdns-browser-arch-aur-builder:v1@sha256:d344e8162f5bcf9bb52750adee123f17eeaa565138bf75096b58e97bad00db0e
    strategy:
      fail-fast: false
      matrix:
        package:
          - name: mdns-browser-bin
            needs_exe: "true"
            generate_script: "./packaging/aur/generate-mdns-browser-bin.sh"
          - name: mdns-browser
            needs_exe: "false"
            generate_script: "./packaging/aur/generate-mdns-browser.sh"
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: 🔢 Get sha256sums
        id: sha256sums
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4.0.0
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.release-info.outputs.tagName }}
          PACKAGE_NAME: ${{ matrix.package.name }}
          RELEASE_VERSION: ${{ needs.release-info.outputs.version }}
          NEEDS_EXE: ${{ matrix.package.needs_exe }}
        with:
          timeout_minutes: 5
          max_attempts: 5
          retry_wait_seconds: 10
          warning_on_retry: true
          command: ./packaging/aur/resolve-sha256.sh

      - name: 📐 Lint generated PKGBUILD
        uses: hrzlgnm/actions/.github/actions/retry@9a8b61da03cbf93775f3fb47f6b393d06d8c62ed # v2.10.0
        env:
          SHA256_EXE: ${{ steps.sha256sums.outputs.sha256_exe }}
          TAG_NAME: ${{ needs.release-info.outputs.tagName }}
          NEEDS_EXE: ${{ matrix.package.needs_exe }}
          GENERATE_SCRIPT: ${{ matrix.package.generate_script }}
          RELEASE_VERSION: ${{ needs.release-info.outputs.version }}
          SHA256: ${{ steps.sha256sums.outputs.sha256 }}
        with:
          # su without --login keeps the step env and only resets
          # HOME/SHELL/USER/LOGNAME, so the script reads inputs from env.
          command: su runner -c "./packaging/aur/lint-pkgbuild.sh"
      - name: 🧰 Setup build environment
        uses: hrzlgnm/actions/.github/actions/aur-fix-ownership@dc057d6ab047f38c862870e90c81a192d24ca92e # v2.11.0

      - name: 🛠️ Setup Deployment keys
        if: github.event.release.tag_name
        uses: hrzlgnm/actions/.github/actions/aur-setup-ssh@dc057d6ab047f38c862870e90c81a192d24ca92e # v2.11.0
        with:
          deploy-key: ${{ secrets.AUR_DEPLOY_KEY }}

      - name: 🔄 Clone AUR Repo
        if: github.event.release.tag_name
        uses: hrzlgnm/actions/.github/actions/aur-clone-repo@dc057d6ab047f38c862870e90c81a192d24ca92e # v2.11.0
        with:
          package-name: ${{ matrix.package.name }}

      - name: 🔍 Check version
        if: github.event.release.tag_name
        uses: hrzlgnm/actions/.github/actions/aur-check-version@dc057d6ab047f38c862870e90c81a192d24ca92e # v2.11.0
        with:
          release-version: ${{ needs.release-info.outputs.version }}

      - name: 🌀 Generate PKGBUILD update and publish
        if: github.event.release.tag_name
        uses: hrzlgnm/actions/.github/actions/retry@9a8b61da03cbf93775f3fb47f6b393d06d8c62ed # v2.10.0
        env:
          SHA256_EXE: ${{ steps.sha256sums.outputs.sha256_exe }}
          TAG_NAME: ${{ needs.release-info.outputs.tagName }}
          NEEDS_EXE: ${{ matrix.package.needs_exe }}
          GENERATE_SCRIPT: ${{ matrix.package.generate_script }}
          RELEASE_VERSION: ${{ needs.release-info.outputs.version }}
          SHA256: ${{ steps.sha256sums.outputs.sha256 }}
        with:
          command: su runner -c "./packaging/aur/publish-aur.sh"
