# Copyright 2025-2026 hrzlgnm
# SPDX-License-Identifier: MIT-0

name: Arch linux AUR

on:
  workflow_call:
  workflow_dispatch:
  release:
    types: [released]

jobs:
  release-info:
    permissions:
      packages: read
    runs-on: ubuntu-slim
    outputs:
      version: ${{ steps.version.outputs.version }}
      tagName: ${{ steps.version.outputs.tagName }}
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1

      - name: 🏷️ Get version info
        id: version
        uses: ./.github/actions/latest-release-info

  update-aur:
    needs:
      - release-info
    permissions:
      packages: read
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/hrzlgnm/mdns-browser-arch-aur-builder:v1@sha256:d06a3c63493066b249b3bdb8b0e503a0936eecaa06259c94a5f466e7a08135d5
    strategy:
      fail-fast: false
      matrix:
        package:
          - name: mdns-browser-bin
            checksum_url: "https://github.com/hrzlgnm/mdns-browser/releases/download/${{ needs.release-info.outputs.tagName }}/mdns-browser_${{ needs.release-info.outputs.version }}_amd64.deb.sha256"
            checksum_url_exe: "https://github.com/hrzlgnm/mdns-browser/releases/download/${{ needs.release-info.outputs.tagName }}/mdns-browser_linux_x64.sha256"
            generate_script: "./packaging/aur/generate-mdns-browser-bin.sh"
          - name: mdns-browser
            checksum_url: "https://github.com/hrzlgnm/mdns-browser/releases/download/${{ needs.release-info.outputs.tagName }}/mdns-browser-v${{ needs.release-info.outputs.version }}.tar.gz.sha256"
            checksum_url_exe: ""
            generate_script: "./packaging/aur/generate-mdns-browser.sh"
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1

      - name: 🔢 Get sha256sums
        id: sha256sums
        shell: bash
        run: |
          URL="${{ matrix.package.checksum_url }}"
          echo "getting $URL"
          SUM=$(curl -LfsS "$URL" | cut -f1 -d ' ')
          echo "sha256=$SUM" >> "$GITHUB_OUTPUT"
          if [[ -n "${{ matrix.package.checksum_url_exe }}" ]]; then
            URL_EXE="${{ matrix.package.checksum_url_exe }}"
            echo "getting $URL_EXE"
            SUM_EXE=$(curl -LfsS "$URL_EXE" | cut -f1 -d ' ')
            echo "sha256_exe=$SUM_EXE" >> "$GITHUB_OUTPUT"
          fi

      - name: 📐 Lint generated PKGBUILD
        shell: bash
        run: |
          su runner -c '
            set -o errexit
            mkdir -p ~/lint
            o=$(pwd)
            EXE_CHECKSUM="${{ steps.sha256sums.outputs.sha256_exe }}"
            ${{ matrix.package.generate_script }} "${{ needs.release-info.outputs.version }}" "${{ steps.sha256sums.outputs.sha256 }}" "${EXE_CHECKSUM:-}" > ~/lint/PKGBUILD
            cd ~/lint
            "$o/packaging/aur/makepkg-lint.sh"
          '
      - name: 🛠️ Setup Deployment keys
        if: github.event.release.tag_name
        shell: bash
        env:
          AUR_DEPLOY_KEY: ${{ secrets.AUR_DEPLOY_KEY }}
        run: |
          chown -R runner:runner .
          su runner -c '
            set -o errexit
            mkdir -p ~/.ssh
            printf '%s\n' "$AUR_DEPLOY_KEY" > ~/.ssh/aur
            chmod 600 ~/.ssh/aur
            echo "Host aur.archlinux.org" >> ~/.ssh/config
            echo "  IdentityFile ~/.ssh/aur" >> ~/.ssh/config
            echo "  User aur" >> ~/.ssh/config
            ssh-keyscan -H aur.archlinux.org >> ~/.ssh/known_hosts
          '

      - name: 🔄 Clone AUR Repo
        if: github.event.release.tag_name
        shell: bash
        run: |
          su runner -c '
            set -o errexit
            git clone --depth=1 "ssh://aur@aur.archlinux.org/${{ matrix.package.name }}.git" ~/aur
          '

      - name: 🔍 Check version
        if: github.event.release.tag_name
        shell: bash
        run: |
          su runner -c '
            set -o errexit
            cd ~/aur
            if [[ -f PKGBUILD ]]; then
              CURRENT_VERSION=$(source PKGBUILD && echo $pkgver)
              if [[ "$(printf "%s\n%s" "$CURRENT_VERSION" "${{ needs.release-info.outputs.version }}" | sort -V | head -n1)" != "$CURRENT_VERSION" ]] || [[ "$CURRENT_VERSION" == "${{ needs.release-info.outputs.version }}" ]]; then
                echo "New version (${{ needs.release-info.outputs.version }}) is not higher than the current version ($CURRENT_VERSION). Exiting."
                exit 1
              fi
            fi
          '

      - name: 🌀 Generate PKGBUILD update and publish
        if: github.event.release.tag_name
        run: |
          su runner -c '
            set -o errexit
            EXE_CHECKSUM="${{ steps.sha256sums.outputs.sha256_exe }}"
            ${{ matrix.package.generate_script }} "${{ needs.release-info.outputs.version }}" "${{ steps.sha256sums.outputs.sha256 }}" "${EXE_CHECKSUM:-}" > ~/aur/PKGBUILD
            cd ~/aur
            if [[ -z $(git status --porcelain) ]]; then
              echo "No changes"
              exit 0
            fi
            makepkg --printsrcinfo > .SRCINFO
            makepkg
            makepkg --install --noconfirm
            git config user.name "hrzlgnm"
            git config user.email "hrzlgnm@users.noreply.github.com"
            git add PKGBUILD .SRCINFO
            git commit -m "New upstream release ${{ needs.release-info.outputs.version }}"
            git push origin master
          '
